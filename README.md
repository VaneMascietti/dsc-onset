# DSC Onset Plot

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt

python src/plot_dsc_onset.py --file data/240614_metoh_nc5_0.111w_0.2rate.txt --direction exo --scale single
